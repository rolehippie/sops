# Standards: 1.2
---
- name: Download sops release
  get_url:
    url: "{{ sops_download }}"
    dest: "{{ sops_path }}/sops"
    owner: root
    group: root
    mode: u=rwx,g=rx,o=rx
  tags:
    - sops

...
